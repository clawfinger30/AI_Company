# HitoriOyakataMemo v0.6.4 いつもの作業 実装記録

日時: 2026-07-13T16:46:40+09:00

## 対象

- アプリ: HitoriOyakataMemo
- バージョン: 0.6.4+73
- 対象タスク: v0.6.4 いつもの作業

## 実装内容

- `usual_works` テーブルをDBバージョン13で追加
- `UsualWork` モデルと `UsualWorkRepository` を追加
- SQLite実装 `SqliteUsualWorkRepository` を追加
- 作業記録メニューに「いつもの作業に保存」を追加
- 作業タブ上部に「いつもの作業から記録」カード導線を追加
- 保存済みの「いつもの作業」から確認型作業記録画面を開けるようにした
- 完了済み現場に紐づく「いつもの作業」は候補に出さない
- DB復元バリデーションをDBバージョン13に対応
- SPEC / TASKS / ROADMAP / バージョン表示を更新

## 検証

- `flutter analyze`: No issues
- `flutter test --reporter compact`: 127件通過
- `git diff --check`: 問題なし
- `TASKS.md` v0.6.4 未完了チェック: 0
- `flutter build apk --debug`: 成功

## 補足

- 「いつもの作業」の削除・並べ替え・専用管理画面は今回の対象外。
- 保存入口は既存作業記録のメニューに限定した。
